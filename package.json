{
  "name": "review-hubs",
  "version": "1.0.0",
  "private": true,
  "description": "Review Hubs coupon marketplace and admin API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check coupons.js && node --check detail.js && node --check detail-api.js"
  },
  "engines": { "node": ">=20" }
}
